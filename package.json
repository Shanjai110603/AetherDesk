{
  "name": "aetherdesk-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=desktop",
    "dev:website": "npm run dev --workspace=website",
    "build": "npm run build --workspace=desktop",
    "build:website": "npm run build --workspace=website",
    "tauri": "tauri"
  }
}
